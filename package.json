{
  "name": "ktv",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:cloud-shared": "node scripts/sync-cloud-shared.js",
    "test": "node scripts/sync-cloud-shared.js --check && node scripts/run-tests.js"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^5.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
