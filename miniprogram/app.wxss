@import "styles/variables.wxss";

page {
  background: var(--page-bg);
  color: var(--text-primary);
  font-size: 28rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

.page {
  width: 100%;
  min-height: 100vh;
  box-sizing: border-box;
}
