.detail-page {
  padding: 24rpx 0 150rpx;
}

.detail-hero {
  position: relative;
  height: 368rpx;
  margin: 0 24rpx;
  overflow: hidden;
  border-radius: 22rpx 22rpx 0 0;
  background:
    radial-gradient(circle at 22% 32%, rgba(255, 71, 225, 0.45) 0, transparent 28%),
    radial-gradient(circle at 80% 62%, rgba(44, 200, 255, 0.35) 0, transparent 30%),
    linear-gradient(145deg, #151229 0%, #070713 100%);
}

.detail-hero__image {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

.detail-hero::after {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(9, 7, 18, 0.02), rgba(9, 7, 18, 0.28));
  content: "";
}

.detail-hero__status {
  position: absolute;
  z-index: 2;
  top: 20rpx;
  left: 22rpx;
  padding: 9rpx 18rpx;
  border-radius: 12rpx;
  background: var(--success);
  color: #ffffff;
  font-size: 24rpx;
  font-weight: 800;
}

.detail-hero__pager {
  position: absolute;
  z-index: 2;
  right: 24rpx;
  bottom: 24rpx;
  color: #ffffff;
  font-size: 25rpx;
}

.detail-card,
.detail-section {
  margin: 0 24rpx 22rpx;
  padding: 28rpx;
  border-radius: 22rpx;
  background: #ffffff;
  box-shadow: var(--shadow-card);
}

.detail-card {
  margin-top: 0;
  border-top-left-radius: 0;
  border-top-right-radius: 0;
}

.detail-card__title {
  display: block;
  color: var(--text-primary);
  font-size: 38rpx;
  font-weight: 900;
}

.detail-card__desc {
  display: block;
  margin-top: 12rpx;
  color: var(--text-secondary);
  font-size: 25rpx;
}

.detail-card__tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin: 24rpx 0;
}

.detail-card__tags text {
  padding: 8rpx 18rpx;
  border-radius: 12rpx;
  background: var(--brand-soft);
  color: var(--brand);
  font-size: 24rpx;
  font-weight: 700;
}

.detail-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 22rpx 0;
}

.detail-row + .detail-row {
  border-top: 1rpx solid var(--border-color);
}

.detail-row__icon {
  width: 36rpx;
  height: 36rpx;
  flex: 0 0 36rpx;
}

.detail-row__main,
.detail-row__sub {
  display: block;
}

.detail-row__main {
  color: var(--text-primary);
  font-size: 28rpx;
  font-weight: 700;
}

.detail-row__sub {
  margin-top: 8rpx;
  color: var(--text-secondary);
  font-size: 24rpx;
}

.detail-row__pill {
  margin-left: auto;
  padding: 12rpx 18rpx;
  border-radius: 999rpx;
  background: var(--brand-soft);
  color: var(--brand);
  font-size: 24rpx;
  font-weight: 800;
}

.detail-section__title {
  display: block;
  margin-bottom: 20rpx;
  color: var(--text-primary);
  font-size: 30rpx;
  font-weight: 900;
}

.host {
  display: flex;
  align-items: center;
  gap: 18rpx;
}

.host__avatar {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 70rpx;
  height: 70rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #ffd1b1, #8f6cff);
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 900;
}

.host__body {
  min-width: 0;
  flex: 1;
}

.host__name,
.host__intro,
.detail-section__text {
  display: block;
}

.host__name {
  color: var(--text-primary);
  font-size: 28rpx;
  font-weight: 800;
}

.host__intro,
.detail-section__text {
  margin-top: 8rpx;
  color: var(--text-secondary);
  font-size: 25rpx;
  line-height: 1.6;
}

.host__arrow {
  color: var(--text-muted);
  font-size: 44rpx;
}

.actions {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: grid;
  grid-template-columns: 120rpx 1fr 1fr;
  gap: 18rpx;
  padding: 18rpx 24rpx 28rpx;
  background: #ffffff;
  box-shadow: 0 -12rpx 30rpx rgba(38, 43, 90, 0.08);
}

.actions button {
  height: 76rpx;
  margin: 0;
  border-radius: 18rpx;
  font-size: 27rpx;
  font-weight: 900;
  line-height: 76rpx;
}

.actions__favorite {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  background: #ffffff;
  color: var(--text-primary);
  font-size: 22rpx;
  line-height: 1.1;
}

.actions__favorite image {
  width: 34rpx;
  height: 34rpx;
  margin-bottom: 4rpx;
}

.actions__secondary {
  border: 2rpx solid var(--brand);
  background: #ffffff;
  color: var(--brand);
}

.actions__primary {
  background: linear-gradient(135deg, #8c66ff, #5237f2);
  color: #ffffff;
}
